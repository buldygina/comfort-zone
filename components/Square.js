import React from 'react';
const Square = ({ textSquare }) => {
    return (
        <div className="square">
    <span className="textSquare">{textSquare}</span>
        </div>
    );
}

export default Square;
